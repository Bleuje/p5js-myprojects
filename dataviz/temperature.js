
function temperature_value(height,xpos,type) {
    this.h = height;
    this.x = xpos;
    
    this.seed = random(10,1000);
    
    this.show = function(){
        if(mouseY<=cnv.height&&mouseY>=0){
            
        }
    }
}